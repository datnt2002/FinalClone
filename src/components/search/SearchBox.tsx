import React from "react";
import { Input } from "../ui/input";
import { ImageIcon } from "@radix-ui/react-icons";
type Props = {};

const SearchBox = (props: Props) => {
  return <Input placeholder="Enter thing to search" />;
};

export default SearchBox;
